riot.tag('btn-group', '<div class="btn-group"> <btn each="{button in buttons}" classname="{button}" text="{button}" type="button"></div> </div>', function(opts) {

    this.buttons = ['default', 'primary', 'success', 'info', 'warning', 'danger', 'link'];


});

riot.tag('btn-sizes', '<div class="bs-example"> <btn each="{button in buttons}" classname="{button.classname}" text="{button.text}" size="{button.size}"></div> </div>', function(opts) {

    this.buttons = [
        {classname: 'primary', text: 'Primary button text', size: 'lg'},
        {classname: 'success', text: 'Success button text'},
        {classname: 'info', text: 'Info button text', size: 'sm'},
        {classname: 'warning', text: 'Warning button text', size: 'xs'}
    ];


});

riot.tag('buttons', '<btn each="{button in buttons}" classname="{button}" text="{button}"></btn> <btn-sizes ></btn-sizes>', function(opts) {

    this.buttons = ['default', 'primary', 'success', 'info', 'warning', 'danger', 'link'];


});

riot.tag('btn', '<button class="{classname}" type="{type}">{ opts.text }</button>', function(opts) {

    var classname = opts.classname.split(' ');

    <!-- ClassNames: default, primary, success, info, warning, danger, link -->
    this.classname = 'btn btn-' + classname.join(' btn-');

    <!-- Types: button, submit, etc. -->
    this.type = opts.type ? opts.type : 'button';

    <!-- Size: lg, sm, xs -->
    this.classname += opts.size ? ' btn-' + opts.size : '';


});
